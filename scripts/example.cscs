DEFINE x type d size 10;
x = "abc";            // ? Date mismatch
x = TODAY();          // ? OK
x = 2025;            // ? Not a date

DEFINE y type n size 5;
y = "def";            // ? Not numeric

function button1@clicked() {
    MessageBox("Hello!");
}