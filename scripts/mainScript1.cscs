CreateWindow(strtrim(tpath()) + "mainScript1.xaml");

function mainScript1_onDisplay(){
    MessageBox("0");
    include(strtrim(tpath()) + "includedScript1.cscs");
    MessageBox("5"); // displays -> not ok!
}