MessageBox("...Testing AppendLine...");

DEFINE filename type a size 1000;
DEFINE line type a size 1000;

filename = "D:\testAppendLine.txt";

line = "ABCDEFGHI";
AppendLine(filename, line);

line = "BBBBBBBBB";
AppendLine(filename, line);

quit;