x = 10;    // x is inferred as an integer //sa var ne radi dole
y = 20.5;  // y is inferred as a floating-point number
name = "AAA"; // name is inferred as a string

MSG(x + y);  // Outputs: 30.5
//Print("Hello, " + name); // Outputs: Hello, AAA

MSG("END");