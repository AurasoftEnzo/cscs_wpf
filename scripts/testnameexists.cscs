MessageBox("...Testing nameexists...");

x = 10;

y = {};
y.Add(100);
y.Add(200);
y.Add(300);

if (nameexists("x")) 
{
    MessageBox("Variable 'x' already exists.");
}

if (nameexists("y")) 
{
    MessageBox("Variable 'y' already exists.");
}

if (nameexists("z")) 
{
    MessageBox("Variable 'z' already exists.");
}

MessageBox("END");

quit;